# AGENTS.md

このリポジトリで作業するAIエージェントは以下に従ってください。

1. まず計画を立てる
2. 小さく実装して都度確認する
3. プロンプトをコードに埋め込まない（`prompts/`配下を使う）
4. 既存構造を壊さず拡張する
5. 失敗時は原因をログまたはドキュメントに記録する
6. 出力形式（JSON/Markdown/TXT）を安定化する
7. テストを先に壊さない
8. 生成品質より再現性と検証可能性を優先する

追加指針:
- run単位成果物を必ず保存する
- 変更は小さな論理単位で行う
- TODOは明示的に残す
